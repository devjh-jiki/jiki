# Codex Adapter

> 한국어: [README.ko.md](./README.ko.md)

Codex-specific configuration and documentation live here:

- [`mcp/`](./mcp) — Codex MCP setup and `config.toml` examples.

Codex discovery entry points stay at the repository root:

- [`AGENTS.md`](../../AGENTS.md) — durable repository guidance.
- [`.agents/skills/`](../../.agents/skills) — relative links to compatible shared skills.

Add Codex-only custom agent TOML files under `agents/` if this repository later introduces runtime-specific subagents. Do not create them merely to mirror Claude Code assets.
