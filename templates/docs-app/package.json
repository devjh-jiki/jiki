{
  "name": "@devjh/docs-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Reusable, token-driven static docs builder (design-system skill based). Markdown in, zero-dependency static site out — Mermaid via CDN.",
  "bin": {
    "docs-app": "build.mjs"
  },
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --serve"
  },
  "dependencies": {
    "highlight.js": "^11.10.0",
    "markdown-it": "^14.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
