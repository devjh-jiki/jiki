---
description: Load the design-system skill and apply it (design / implement / review UI)
---

You are going to perform a design-system task. First, fully load the skill:

Use your Read tool to read this file and follow it:
`/Users/lsy6234naver.com/Desktop/dev-hub/skills/engineering/design-system/SKILL.md`

Then read its `references/` files in the same directory as the skill directs (astryx-foundations, anti-slop-and-review, stacks, docs-template-guide), and use the bundled `assets/docs-template/` when the deliverable is a documentation site.

Then carry out this request:

$ARGUMENTS

Reminders from the skill that matter most:
- If this project already has a design system (tokens/theme/Tailwind config/component library), RESPECT it — extend it, don't impose Astryx.
- Tokens are the contract: two-tier (base → semantic), components reference semantic only.
- For technical docs, default to the cool-neutral + one-accent + separate code palette.
- Accessibility is non-negotiable (WCAG AA contrast, ≥44px targets, ≥16px body).
